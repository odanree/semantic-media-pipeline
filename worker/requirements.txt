# PyTorch + DirectML (GPU via DirectX 12 / /dev/dxg on WSL2)
# torch-directml pins torch==2.4.1 and torchvision==0.19.1 — do NOT upgrade independently
# torch-directml is Windows-only — skipped automatically on Linux/ARM64
torch==2.4.1
torchvision==0.19.1
torch-directml==0.2.5.dev240914; sys_platform == "win32"

celery[redis]==5.6.2
redis==5.2.1
sentence-transformers==5.2.3
qdrant-client==1.17.0
SQLAlchemy==2.0.47
asyncpg==0.30.0
psycopg2-binary==2.9.10
Pillow==10.4.0
pillow-heif==0.14.0
opencv-python==4.13.0.92
tqdm==4.67.1
python-dotenv==1.0.1
pydantic==2.10.6
boto3==1.34.136
google-cloud-storage==2.16.0
ffmpeg-python==0.2.0
psutil==6.0.0

# Audio feature extraction (audio_extractor.py)
# librosa pins numpy<2; consistent with api/requirements.txt
librosa>=0.10.2
soundfile>=0.12.1
scipy>=1.13.0

# YOLO object detection (yolo_detector.py)
ultralytics>=8.0.0
# OCR for visual feature extraction (optional — skipped gracefully if absent)
# pytesseract requires tesseract-ocr system package: apt-get install tesseract-ocr
# Uncomment if desired:
# pytesseract>=0.3.10
prometheus-client==0.20.0
